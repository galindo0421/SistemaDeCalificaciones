#include <stdio.h>
#include <string.h>
#include "admin.h"
#include "login.h"

int main() {
    /*if(esAdmin(usuario, contrasena)){
        menuGestionAdmin();
    }else if(login()){
        menuDocente();
    }else if (login()){
        menuEstudiante(estudiante[0]);
    }*/
    //saludoBienvenida();
    //manejarLogin();
    menuEstudiante();
    return 0;
}
